#include "main.h"

/**
 * main - checks the code
 * swap_int - swaps the values of two integers
 * @a: integer to swap
 * @b: integer to swap
 */

void swap_int(int *x, int *y)
{
	int m;
	m = *a;
	*a = *b;
	*b = m;
}
